import SwiftUICore
import SwiftUI

struct VaultCard: View {
    var body: some View {
        Button(action: {}) {
            Text("Open a vault")
        }
        .buttonStyle(.bordered)
        .cornerRadius(8)
        .padding()
    }
}
